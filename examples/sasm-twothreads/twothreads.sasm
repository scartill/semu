/// User thread functions

// Main thread
// input: a - 'A' or 'B' symbol
FUNC main
    int a
    nop
    ldr &main c
    jmp c

// Secondary thread
FUNC second
    int a
    push a
    CALL ASuspend
    pop a
    nop
    ldr &second c
    jmp c
